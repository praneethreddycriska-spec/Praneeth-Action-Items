VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_DB_URL=

# Not read by the app directly — the AI Action Mic's Groq key is stored
# server-side in Supabase Vault and called from the ai_parse_action() Postgres
# function (see supabase/migrations/0002_groq_ai_parse.sql), never in the
# frontend bundle. To (re)configure it, run in the SQL editor:
#   select vault.create_secret('gsk_...', 'groq_api_key');
GROQ_API_KEY=
